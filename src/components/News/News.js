import React from "react";
import classes from './News.module.scss'

const News = (props) => {
    return (
        <div className={classes.News}>
            News
        </div>
    );
};

export default News;